name: Test Action

on:
  push:
    branches:
      - main  # Runs the action whenever you push to the main branch

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: 22f3000910@ds.study.iitm.ac.in
        run: echo "Hello, world!"
